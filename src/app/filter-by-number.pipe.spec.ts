import { FilterByNumberPipe } from './filter-by-number.pipe';

describe('FilterByNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
